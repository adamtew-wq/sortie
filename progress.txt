# Ralph Progress Log

This file is appended to by each Ralph iteration. The agent reads it at the
start of each loop to understand prior state and avoid repeating work.

Format per entry:
  [YYYY-MM-DD HH:MM] Issue #N — <short outcome>

---

[2026-06-15 22:35] Issue #2 — Framework spec authored (7 files in framework/): session-schema.json, exercise-library.json (77 exercises, 60 home-friendly, all 5 modalities + all 7 movement patterns), session-templates.json (5 weights spine slots + run/swim/bike/conditioning archetypes, 4–6 week accessory rotation), progression-rules.md (+2.5 kg/2-fail-stall/3-stall-90%-deload + cardio + readiness rules), load-balancing.md (per-modality weekly caps, hard/easy spacing, training-load formula, readiness multipliers), injury-routing.md (knee/shoulder/lower-back/hip/ankle substitution map), rehab-protocols.json (3 placeholder protocols). Added Node test suite (no installs needed): 16 tests covering structural acceptance criteria + 5 sample sessions validated against schema, equipment profiles, and niggle routing. All green. Commit d08e086 pushed to main; GitHub closed issue #2 via Closes #2.

[2026-06-16 00:10] Issue #4 — PWA shell landed. TDD: wrote tests/pwa-shell.test.js (7 tests, red), then implemented the five shell files. app/index.html (viewport + theme-color + apple-mobile-web-app-* meta, #app root, links manifest/style.css/app.js), app/manifest.json (name "Sortie", standalone display, dark #0a0a0c theme/background, SVG icon with "any maskable" purpose), app/sw.js (versioned cache "sortie-shell-v1", precaches the 5 shell files + ./ + icon on install, cache-first for shell, network-first for api.github.com so logged data stays fresh, cache cleanup on activate, clients.claim), app/style.css (dark theme, system-ui, 48px tap targets, safe-area-inset padding, box-sizing reset), app/app.js (feature-detects serviceWorker before registering ./sw.js after load, renders "Sortie — Loading…" placeholder into #app), app/icons/icon.svg (placeholder mark). Deleted obsolete app/.gitkeep. Full suite 23/23 green. Commit 92ee4a4 pushed to main; GitHub closed issue #4 via Closes #4.

[2026-06-16 02:25] Issue #5 — Deterministic engine landed. TDD: wrote tests/engine.test.js (10 tests covering all AC, red), then implemented app/engine/index.js as a pure function generateSession(inputs, framework, data) → Session. Engine encodes the framework rules: spine progression (+2.5 kg on success, 2-fail stall/repeat, 3-stall deload to 90% rounded to 2.5 kg) via computeSpineState walking history forward; modality selection by attribute wants minus 7-day weekly-minutes ratio per modality (cap-normalised) — verifies 3 recent weights sessions push the next pick off weights; weights session-type picker drops slots where ≥50% of spine patterns are flagged (knee niggle routes off lower); equipment filter walks each exercise's equipment[] vs the active profile (Home / Military / ad-hoc array); spine substitution map (Back squat→Goblet squat, Bench press→DB bench press, etc.) when kit missing or pattern flagged; rehab weave looks up niggle.region → injury-routing rehabId → rehab-protocols.json and prepends to warmup with block.rehab:true; readiness multipliers from load-balancing.md (5/4→1.0, 3→0.85/0.95, 2→0.70/0.90, 1→0.50/0.80 + 25-min cap) scale sets, accessory count, and intensity; cardio archetype picker (run long ≥75 min budget, else easy; bike long ≥90; swim easy; conditioning kb-circuit). Output validates against framework/session-schema.json (additionalProperties:false honoured throughout — only known block fields emitted). Full suite 33/33 green. Commit pushed; GitHub closed issue #5 via Closes #5.

[2026-06-16 05:30] Issue #7 — Tap-to-confirm session logging landed. TDD: wrote tests/ui-logging.test.js (15 tests, red), then implemented the slice across app/ui/state.js + app/app.js. State machine extended with logging substate seeded by START from state.suggestion.main (one actuals row per main block, each carrying a `sets[]` checklist whose entries start `{reps, load, done:false, skipped:false}` from the engine's prescription). New actions: LOG_DONE_SET (marks current set done and advances the cursor via advanceCursor, hopping over already-skipped exercises; falls into stage='summary' when the cursor walks off the end), LOG_EDIT_SET (in-place patch by exerciseIndex/setIndex — one tap, no extra confirm step), LOG_SKIP_EXERCISE (flags skippedExercise + auto-marks non-done sets as skipped, advances over any subsequent skipped rows), LOG_END_EARLY (sets endedEarly:true, jumps to summary preserving only the sets the user actually confirmed), LOG_SET_RPE (captures 1–10), LOG_SAVE (builds the entry, persists via injected storage, lands on stage='saved'). buildLogEntry(state, endedAt) emits a history-shaped record — `{date, modality, sessionType, duration (rounded minutes from startedAt→endedAt), equipmentProfile, readiness, rpe, endedEarly, exercises: [...]}` — each exercise summarised with `name, sets (count done), reps/load (last completed set), success (all sets done && !skipped), skipped, actualSets[]`; success=true is exactly the spine-progression trigger the engine reads via computeSpineState. appendHistoryEntry/loadLocalHistory use a `sortie.history` key with JSONL semantics — always append a new line, never rewrite existing lines (matches data/history.jsonl conventions). renderLogging shows the modality badge + a 1Hz timer (`data-elapsed-from` consumed by app.js setInterval; only ticks while stage='logging', cleared on every other stage), exercise name, "Set N of M", inline +/− steppers for load (2.5 kg) and reps (1) with data-edit-load=up|down + data-edit-reps=up|down (one tap = one edit, satisfying the ≤2-tap edit AC), big Done ✓ primary target, and Skip exercise / End session early in the overrides grid. renderSummary lists every touched exercise (done sets or skipped), offers 1–10 RPE chips and a Save button. renderSaved confirms "Session logged." app/app.js wires it all: START now passes startedAt:Date.now(); click delegation extended for data-edit-load, data-edit-reps, data-rpe and the four new actions; localStorageOrNull() guards SSR-style absence; loadResources merges file-based history.jsonl with loadLocalHistory(localStorage), date-sorted, so the very next engine run reflects the logged loads (AC: progression actually advances). sw.js bumped to sortie-shell-v3 so old clients drop stale ui/state.js. style.css adds the logging/summary/saved sections — ≥48px targets, dark theme preserved. The progression-advancement AC is asserted directly: a test logs a successful session via the reducer, then computeSpineState(baselines, [entry]) shows +2.5 kg vs the baselines-only state. Full suite 64/64 green. Commit pushed to main; GitHub closed issue #7 via Closes #7.

[2026-06-16 03:50] Issue #6 — Daily loop UI landed. TDD: wrote tests/ui-checkin.test.js (12 tests) + tests/ui-shell.test.js (4 tests), red, then implemented in two layers so the UI stays testable without a DOM. app/ui/state.js is a pure state machine + render helpers: initialState() returns stage:'checkin' with null answers; reducer() handles SET_ENERGY/SORENESS/SLEEP/LOCATION/FOCUS, SHORTEN, TOGGLE_PICKER, GO/REGENERATE (run engine + advance to 'suggestion'), START (→'logging'); mapCheckinToInputs() converts {energy, soreness, sleep} → readiness (energy + sleep mod, clamped 1–5) + activeNiggles (none/upper→shoulder+push/lower→knee+squat/full→both, each with rehabProtocolId); renderCheckin() emits chip buttons (data-energy=1..5, data-soreness=none/upper/lower/full, data-sleep=poor/ok/good, aria-pressed reflects selection) + Go — no <input>/textarea, three taps and Go; renderSuggestion() emits modality badge + duration + location + rationale + <details data-block="warmup"|"cooldown"> (collapsed by default) + main section + override bar (data-action=change-focus/change-location/shorten/regenerate) + Start; rehab blocks carry class="block rehab" so they're visually distinct. app/app.js loads framework/data via fetch (with offline fallback to cache), registers SW, delegates clicks on #app to dispatch reducer actions; ad-hoc location picker collects ticked equipment keys and commits on "Use this kit". index.html switched to <script type="module">; sw.js bumped to sortie-shell-v2 and now precaches ui/state.js, engine/index.js, and the framework/data JSON so the daily loop runs offline on first visit. style.css adds chip rows (min-height 48px), modality badge, rationale callout, override grid, rehab tag — all ≥44px tap targets, dark theme preserved. 16 new tests cover all AC: 3-tap-no-typing check-in, sleep modulates readiness (energy 2 + poor sleep → readiness 1), lower soreness emits a knee niggle, ad-hoc subset constrains every block to the ticked kit, regenerate with a different focus produces a different modality, Shorten caps duration, rehab blocks tagged, override bar complete. Full suite 49/49 green. Commit 82e3def pushed to main; GitHub closed issue #6 via Closes #6.
